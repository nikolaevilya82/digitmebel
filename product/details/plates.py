class Panel:
    def __init__(self, name, color, thickness, width, depth, height):
        self.name: str = name
        self.color: str = color
        self. thickness: int = thickness
        self.width: int = width
        self.depth: int = depth
        self.height: int = height
